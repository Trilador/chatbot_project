PYTHON_CODE_DB = {
    "for loop": (
        "for i in range(10):\n    print(i)",
        "A basic for loop in Python that prints numbers from 0 to 9."
    ),
    "while loop": (
        "count = 0\nwhile count < 10:\n    print(count)\n    count += 1",
        "A basic while loop in Python that prints numbers from 0 to 9."
    ),
    "if statement": (
        "x = 5\nif x > 0:\n    print('x is positive')",
        "A basic if statement in Python that checks if a number is positive."
    ),
    "if-else statement": (
        "x = -5\nif x > 0:\n    print('x is positive')\nelse:\n    print('x is not positive')",
        "An if-else statement in Python that checks if a number is positive or not."
    ),
    "list comprehension": (
        "[x**2 for x in range(10)]",
        "A list comprehension in Python that generates a list of squares of numbers from 0 to 9."
    ),
    "function definition": (
        "def greet(name):\n    return f'Hello, {name}!'",
        "A basic function definition in Python that greets the provided name."
    ),
    "lambda function": (
        "square = lambda x: x**2",
        "A lambda function in Python that returns the square of a number."
    ),
    "dictionary": (
        "person = {'name': 'Alice', 'age': 30}",
        "A basic dictionary in Python representing a person with a name and age."
    ),
    "try-except": (
        "try:\n    x = 1/0\nexcept ZeroDivisionError:\n    print('Cannot divide by zero')",
        "A try-except block in Python that handles division by zero error."
    )
}
